# Resolving Git Merge Conflicts

If you are seeing merge conflicts when trying to pull the code to your local computer, follow these steps.

## Option 1: I want to keep the Sandbox version (Recommended)
If you want your local computer to match exactly what is in this Sandbox (AgriVikas), run these commands in your local terminal:

